import { FC } from 'react'
import { AuthenticationRegisterContainer } from '@/modules/authentication/ui/register/authentication-register-container'

const Page: FC = () => {
  return <AuthenticationRegisterContainer />
}

export default Page
