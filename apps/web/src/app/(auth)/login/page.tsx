import { FC } from 'react'
import { AuthenticationLoginContainer } from '@/modules/authentication/ui/login/authentication-login-container'

const Page: FC = () => {
  return <AuthenticationLoginContainer />
}

export default Page
